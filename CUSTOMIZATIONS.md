# Personal Fork Customizations

This ledger records intentional differences from `NousResearch/hermes-agent`.

## Baseline

- Personal fork baseline: `214623389a37dc9608cc54cb8910d35d0ab12197`
- Baseline purpose: current upstream Hermes runtime with Volmarr-specific documentation layered
  on top.
- Runtime divergence at Milestone 0: **none**.
- Test divergence at Milestone 0: **none**.

## Intentional Differences

| Area | Difference | Reason | Upstream surface touched |
|---|---|---|---|
| Project identity | Personal `README.md` and RuneForgeAI project documents | Define the fork's purpose, culture, and roadmap | Documentation only |
| Upstream intake | `scripts/sync_upstream.py` preserves `README.md` byte-for-byte | Upstream documentation must not overwrite the fork's identity | Maintenance workflow only |
| Architecture | `ARCHITECTURE_VOLMARR.md` defines personal domain boundaries | Prevent project-specific systems from leaking into Hermes core | Documentation only |
| Licensing | Component and notice ledgers track provenance per imported system | Preserve source-specific licenses and attribution | Documentation only |

## Runtime Customizations

| Domain | Behavior | Hermes surface | Verification | Upstream-sync risk |
|---|---|---|---|---|
| Events | Opt-in `volmarr-core` plugin publishes versioned session, turn, and tool lifecycle metadata to Verðandi on Unix-socket-capable hosts; native Windows safely drops events until Verðandi gains a compatible transport | General plugin hooks only | Real discovery, privacy contract, offline-hub and unsupported-platform fail-open tests | Low; additive plugin directory, no core patch |
| Health | `hermes volmarr health [--json]` proves Verðandi responsiveness with a bounded `ping`/`pong` exchange scoped to the active profile | General plugin CLI registration | Real discovery, valid-pong and protocol-error contracts | Low; additive command owned by the plugin |
| Cognition endpoint | `hermes volmarr cognition health [--json]` validates a profile-scoped, loopback-only A.E.S.I.R. model catalog and credential file | General plugin CLI registration plus external HTTP protocol adapter | Real loopback server with A→B→A profile isolation and remote-host refusal | Low; no Hermes provider or core patch |
| Cognition routing | Versioned metadata-only contract chooses deterministic, local, or cloud capability tiers without inspecting content or selecting cloud providers | General plugin CLI registration and internal application contract | Real discovery, precedence table, bounds, strict types, and content-field rejection | Low; additive plugin modules only |
| Cognition telemetry | Accepted route decisions emit versioned, content-free deterministic/local/cloud events through Verðandi | Existing plugin CLI and Verðandi adapter | Event-type mapping, versioned schema, privacy contract, and fail-open transport | Low; reuses the existing external-service boundary |
| Manual cognition modes | Routing schema v2 adds truthful `auto`, forced-`local`, and forced-`deep` modes while retaining v1 compatibility; impossible forced-local requests are blocked instead of escalated | Existing cognition routing contract | Compatibility, precedence, local-capability conflict, failure, and input-bound contracts | Low; no main-loop or provider mutation |
| Local reflex execution | `hermes volmarr cognition execute` runs bounded, tool-free local text through A.E.S.I.R. or returns an explicit deterministic/cloud/blocked directive; proxy and redirect forwarding are disabled and outcome telemetry excludes content | Existing plugin CLI, cognition adapter, router, and Verðandi publisher | Real loopback completion, exact request shape, privacy telemetry, explicit escalation, forced-local failure, and declared-size contracts | Low; additive plugin application layer, no Hermes provider or core patch |
| Bifröst attachment | `hermes volmarr memory health [--json]` constructs the external Bifröst package with active-profile Mímir/Muninn paths and validates Mímir read-only; optional remote and associative backends stay disabled | General plugin CLI and external package adapter | Real discovery, A→B→A profile isolation, Mímir schema/count, no-create, and path-escape contracts | Low; no memory-provider registration or core patch |
| Present State | The opt-in plugin persists bounded current profile/session facts, injects them on the per-turn user side, commits only completed turns, and mirrors successful built-in memory writes | Existing `pre_llm_call`, `post_llm_call`, `on_session_end`, `on_session_start`, and `post_tool_call` hooks | Real discovery, completed/interrupted verdicts, memory-write success gate, corruption recovery, context fencing, and A→B→A profile isolation | Low; adapted out of the former core patch and contained in one plugin module |
| MemPalace attachment | `hermes volmarr memory mempalace health [--json]` validates official MemPalace v3.10.0+ and the active profile's verbatim episodic store without opening ChromaDB | General plugin CLI and external-package/storage adapter | Minimum official version, package identity, A→B→A profile isolation, SQLite integrity, collection presence, path escape, and byte-for-byte no-mutation contracts | Low; no retrieval, ingestion, memory-provider registration, or core patch |
| OpenViking attachment | `hermes volmarr memory openviking health [--json]` validates the existing bundled provider and an official v0.4.21+ local server without credentials or writes | General plugin CLI health adapter plus the unchanged bundled `openviking` provider | Real discovery, A→B→A profile isolation, official health identity/version, loopback-only URL, response bounds, and no redirect/proxy forwarding | Low; no duplicate provider, activation, retrieval, ingestion, server start, or core patch |
| Hermes SessionDB preservation | `hermes volmarr memory sessiondb health [--json]` audits the fixed active-profile `state.db` read-only while Hermes remains the sole canonical transcript owner | General plugin CLI health adapter; no writer, schema migration, or replacement store | Real discovery, A→B→A isolation, integrity/schema/foreign-key checks, missing-store no-create, and byte-for-byte lifecycle no-mutation contracts | Low; no core patch and no shadow transcript authority |
| Bounded context packets | A versioned central builder deduplicates, prioritizes, provenance-labels, and hard-caps all prompt-facing memory; Present State is its first producer | Existing `pre_llm_call` user-context channel only | Real discovery, deterministic rendering, system-message immutability, hostile fence escaping, section ordering/deduplication, complete-item truncation, and exact global/section bounds | Low; additive plugin module, no system-prompt mutation, and external retrieval remains disabled |
| Preserved affective regulator | Earlier schema-v9 deterministic reward, accountability, safety, continuity, and engineering-quality gauges persist per profile and update only after successful turn verdicts; disabled by default | Existing lifecycle hooks plus a typed contribution to the central user-side context packet | Real discovery, legacy-schema upgrade, representative preserved channels, completed/interrupted gates, safety framing, packet centralization, and A→B→A isolation | Low; earlier core wiring was not copied and no safety/permission policy is modified |
| Verðandi affective stimuli | Enabled affective profiles perform bounded sequence-cursor catch-up for official reward, negative, push, blocker, resolution, and milestone metadata without retaining event content | Existing local Verðandi Unix-socket `recent` contract plus profile-local cursor | Real discovery, exact wire request, first-contact baseline, replay deduplication, strict source/type allowlist, content non-retention, malformed fail-open behavior, and A→B→A cursor/state isolation | Low; no resident thread, no Verðandi source copy, and unavailable hubs never affect turns |
| Synthetic PAD emotional layer | Already-classified regulatory events evolve persistent bounded valence, energy, and agency coordinates with baseline decay and explicit non-consciousness framing | Existing affective lifecycle bridge plus one typed item in the central user-side context packet | Real discovery, positive/failure transitions, completed/interrupted gate, restart persistence, decay, corruption recovery, Verðandi intake, compact rendering, and A→B→A isolation | Low; no raw-text classifier, system-prompt mutation, safety-policy change, or hardware-telemetry coupling |
| WYRD service attachment | `hermes volmarr world health [--json]` validates the official WYRD v1 loopback liveness contract without reading or mutating world data | General plugin CLI plus external HTTP protocol adapter | Real discovery, current official unversioned response, documented versioned response, minimum reported version, A→B→A profile isolation, no credentials, loopback-only URL, bounded response, and redirect/proxy refusal | Low; no WYRD source copy, world tools, storage access, event publication, or prompt injection |
| Read-only WYRD tools | `world_get` retrieves official world snapshots or entity facts; `world_query` requests Passive Oracle context with WYRD turn-loop generation and writeback forcibly disabled | General plugin tool registration plus official `/world`, `/facts`, and context-only `/query` HTTP routes | Real discovery, exact methods/routes/bodies, no credentials, strict IDs, input/output/list bounds, invalid-input preflight, and A→B→A endpoint isolation | Low; no mutation endpoint, automatic prompt injection, shadow world store, or copied WYRD source |
| Relevant WYRD world context | An opt-in, profile-configured persona receives fresh Passive Oracle context for the current user query as one bounded, provenance-bearing `WORLD STATE` item | Existing `pre_llm_call` user-context channel and central context-packet builder; official `/query` route with turn loop/writeback disabled | Real discovery, disabled default, exact request contract, source and global bounds, hostile-fence escaping, system-message immutability, and A→B→A endpoint/context isolation | Low; no system-prompt mutation, automatic storage, WYRD LLM generation, conversation history, or writeback |
| Confirmed WYRD writes and change signals | `world_set` writes official canonical-fact events and `world_observe` writes official observations; content-free Verðandi metadata is published only after WYRD confirms success | General plugin tools, official `POST /event`, and existing profile-local Verðandi publisher | Real discovery, exact fact/observation bodies, strict field bounds, invalid-input preflight, explicit acknowledgement gate, failed-write suppression, and event-content non-retention | Low; no invented move/history API, shadow store, raw world content in Verðandi, or Hermes core patch |
| WYRD restart acceptance | A standalone verifier proves an official WYRD canonical fact survives bridge reconstruction in a disposable SQLite store | Operator script using the installed official `wyrdforge` package directly | Passed against official v1.0.0 commit `9884ce8a9e683dc20f372a91eb66ba5b02561300`; exactly one fact recovered after restart and no live profile/store touched | None at runtime; verifier is opt-in and disposable |
| Stable entity identity | The enabled Volmarr plugin creates one versioned, profile-local `entity/entity.yaml` identity and validates it without tying continuity to a session, model, or provider; `SOUL.md` is never touched | Existing `on_session_start` hook and plugin CLI registration | Real discovery, create-once byte stability, model replacement, A→B→A profile isolation, corrupt-state refusal, path-escape fallback, read-only health, and byte-for-byte `SOUL.md` preservation | Low; additive plugin state and no Hermes core or persona-file mutation |
| Relationship continuity | A versioned `entity/relationships.yaml` ledger stores explicit current relationship state and bounded append-only history under the stable profile identity | Existing lifecycle hook and plugin tool registration (`relationship_get`, `relationship_upsert`) | Real discovery, identity ownership, explicit update/read/archive, bounded restart history, invalid-input preflight, corrupt/wrong-owner refusal, identity-disable dependency, and A→B→A isolation | Low; no conversation mining, prompt injection, implicit trust changes, deletion, or core patch |

Every future entry must name:

- the behavior added or changed;
- its owning domain;
- the exact Hermes extension surface used;
- persistent formats or configuration keys introduced;
- verification performed;
- upstream-sync risk;
- source and license provenance when adapted from another project.
